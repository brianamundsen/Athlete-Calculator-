---
layout: base.njk
title: "Contact Athlete Calculator"
description: "Get in touch with Athlete Calculator — questions, corrections, data suggestions, or partnership inquiries."
canonicalPath: "/contact"
mailerlite: false
navContextUrl: "https://athletecalculator.com"
navContextLabel: "AthleteCalc"
jsonld: |-
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "ContactPage",
    "headline": "Contact Athlete Calculator",
    "description": "Get in touch with Athlete Calculator.",
    "url": "https://athletecalculator.com/contact"
  }
  </script>
permalink: "contact/index.html"
---
<style>
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
  :root {
    --bg: #080c10; --surface: #0f1419; --surface2: #161c24;
    --border: #1e2832; --accent: #aaff00; --accent-dim: rgba(170,255,0,0.12);
    --text: #e8edf2; --text-muted: #5a6470; --text-mid: #8a96a4;
    --font-display: 'Barlow Condensed', sans-serif;
    --font-body: 'DM Sans', sans-serif;
    --font-mono: 'DM Mono', monospace;
  }
  html { scroll-behavior: smooth; }
  body { background: var(--bg); color: var(--text); font-family: var(--font-body); min-height: 100vh; overflow-x: hidden; }

  nav { padding: 1.25rem 2rem; border-bottom: 1px solid var(--border); display: flex; align-items: center; justify-content: space-between; flex-wrap: wrap; gap: 1rem; position: sticky; top: 0; background: rgba(8,12,16,0.95); backdrop-filter: blur(12px); z-index: 100; }
  .nav-brand { font-family: var(--font-display); font-size: 1.3rem; font-weight: 900; text-transform: uppercase; letter-spacing: 0.05em; color: var(--text); text-decoration: none; }
  .nav-brand span { color: var(--accent); }
  .nav-links { display: flex; gap: 1.5rem; flex-wrap: wrap; }
  .nav-links a { font-family: var(--font-mono); font-size: 0.65rem; letter-spacing: 0.1em; text-transform: uppercase; color: var(--text-muted); text-decoration: none; transition: color 0.2s; }
  .nav-links a:hover, .nav-links a.active { color: var(--accent); }

  .hero { max-width: 760px; margin: 0 auto; padding: 3rem 1.25rem 1rem; }
  .breadcrumb { font-family: var(--font-mono); font-size: 0.62rem; letter-spacing: 0.12em; text-transform: uppercase; color: var(--text-muted); margin-bottom: 1.25rem; }
  .breadcrumb a { color: var(--text-muted); text-decoration: none; }
  .breadcrumb a:hover { color: var(--accent); }
  .breadcrumb span { margin: 0 0.4rem; }
  h1 { font-family: var(--font-display); font-size: clamp(2.4rem, 7vw, 4rem); font-weight: 900; line-height: 0.98; text-transform: uppercase; letter-spacing: -0.01em; color: var(--text); margin-bottom: 0.75rem; }
  h1 em { color: var(--accent); font-style: normal; }
  .hero-sub { font-size: 1rem; color: var(--text-mid); margin-bottom: 1rem; line-height: 1.6; }

  .content-wrap { max-width: 760px; margin: 0 auto; padding: 1rem 1.25rem 4rem; }
  h2 {
    font-family: var(--font-display);
    font-size: 1.4rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.02em;
    color: var(--text);
    margin: 2.5rem 0 1rem;
    padding-bottom: 0.6rem;
    border-bottom: 1px solid var(--border);
  }
  p { font-size: 0.95rem; color: var(--text-mid); line-height: 1.7; margin-bottom: 1.1rem; }

  .card-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(220px, 1fr)); gap: 1rem; margin: 1.5rem 0; }
  .card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 14px;
    padding: 1.5rem;
  }
  .card h3 {
    font-family: var(--font-display);
    font-size: 1.1rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.02em;
    color: var(--accent);
    margin-bottom: 0.6rem;
  }
  .card p { margin: 0; font-size: 0.85rem; color: var(--text-mid); }
  .email {
    font-family: var(--font-mono);
    color: var(--accent);
    font-size: 0.9rem;
  }

  .calcs-strip { max-width: 960px; margin: 0 auto; padding: 0 1.25rem 3rem; }
  .calcs-label { font-family: var(--font-display); font-size: 1.6rem; font-weight: 800; text-transform: uppercase; letter-spacing: 0.05em; color: var(--text-muted); margin-bottom: 1.25rem; padding-bottom: 0.75rem; border-bottom: 1px solid var(--border); }
  .calcs-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(180px, 1fr)); gap: 0.75rem; }
  .calc-pill {
    background: var(--accent-dim);
    border: 1px solid rgba(170,255,0,0.2);
    border-radius: 10px;
    padding: 1rem;
    text-decoration: none;
    transition: border-color 0.2s;
  }
  .calc-pill:hover { border-color: var(--accent); }
  .calc-pill-name { font-family: var(--font-display); font-size: 0.95rem; font-weight: 800; text-transform: uppercase; color: var(--accent); margin-bottom: 0.25rem; }
  .calc-pill-desc { font-size: 0.75rem; color: var(--text-mid); line-height: 1.4; }

  footer { text-align: center; padding: 2rem 1.5rem; font-size: 0.75rem; color: var(--text-muted); border-top: 1px solid var(--border); line-height: 1.8; }
  footer a { color: var(--text-muted); text-decoration: none; }
  footer a:hover { color: var(--accent); }
</style>
<div class="hero">
<div class="breadcrumb">
<a href="https://athletecalculator.com">Home</a>
<span>→</span>
    Contact
  </div>
<h1>Get in <em>Touch</em></h1>
<p class="hero-sub">Questions about a calculator, a data correction, or just want to say hi — here's how to reach us.</p>
</div><div class="content-wrap">
<div class="card-grid">
<div class="card">
<h3>General Inquiries &amp; Corrections</h3>
<p>Spotted an outdated combine number, an error in a percentile chart, or have a data source you think we should be using? Email us at <span class="email">contact@athletecalculator.com</span> and we'll take a look. We update our calculators regularly and appreciate corrections.</p>
</div>
<div class="card">
<h3>Partnerships &amp; Press</h3>
<p>For media inquiries, data licensing questions, or partnership proposals, reach out to the same address above with "Partnership" in the subject line and we'll route it appropriately.</p>
</div>
<div class="card">
<h3>Suggest a Calculator</h3>
<p>Have an idea for a calculator or athletic standard we haven't covered yet? We're always looking to expand our tools based on what athletes, coaches, and parents actually need. Send suggestions to the email above.</p>
</div>
</div>
<h2>Response Time</h2>
<p>Athlete Calculator is run by a small, focused team, so please allow a few business days for a response. We read every message, even if we can't reply to all of them individually.</p>
</div><div class="calcs-strip">
<div class="calcs-label">Free Calculators</div>
<div class="calcs-grid">
<a class="calc-pill" href="https://athletecalculator.com/vertical-jump-percentile">
<div class="calc-pill-name">Vertical Jump</div>
<div class="calc-pill-desc">Your percentile vs NBA and general population</div>
</a>
<a class="calc-pill" href="https://athletecalculator.com/can-i-dunk-calculator">
<div class="calc-pill-name">Can I Dunk?</div>
<div class="calc-pill-desc">Find out instantly by height and vertical</div>
</a>
<a class="calc-pill" href="https://athletecalculator.com/sprint-speed-calculator">
<div class="calc-pill-name">Sprint Speed</div>
<div class="calc-pill-desc">Your percentile vs NFL combine data</div>
</a>
<a class="calc-pill" href="https://athletecalculator.com/squat-strength-calculator">
<div class="calc-pill-name">Squat Strength</div>
<div class="calc-pill-desc">Predict your vertical from your squat</div>
</a>
<a class="calc-pill" href="https://athletecalculator.com/broad-jump-sprint-speed-calculator">
<div class="calc-pill-name">Broad Jump Speed</div>
<div class="calc-pill-desc">Estimate your 40 time from your broad jump</div>
</a>
</div>
</div>